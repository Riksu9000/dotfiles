#!/bin/sh

case $BLOCK_BUTTON in
	1) urxvt -e sudo pacman -Syu --noconfirm ;;
	3) notify-send "$(pacman -Qu)" ;;
esac

# FULL_TEXT
#echo "$(pacman -Qu | wc -l | sed -e 's/^0$//g')"

if [ $(pacman -Qu | wc -l) -gt 0 ]; then
	echo ""
fi

# SHORT_TEXT
echo

# FOREGROUND COLOR
#echo "$(xrdb -q | awk '/color14/ {print $2}')"
