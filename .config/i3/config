#  _ _____                  __ _
# (_)___ /  ___ ___  _ __  / _(_) __ _
# | | |_ \ / __/ _ \| '_ \| |_| |/ _` |
# | |___) | (_| (_) | | | |  _| | (_| |
# |_|____/ \___\___/|_| |_|_| |_|\__, |
#                                |___/

set $mod Mod4
floating_modifier $mod
font pango:xos4 Terminus 14
assign [class="Steam"] $ws10
assign [title="Steam"] $ws10


# Border settings
for_window [tiling] border pixel 3
for_window [floating] border pixel 3
hide_edge_borders smart


# COLORS #
# Basic
set_from_resource $black	color0	#000000
set_from_resource $white	color15	#ffffff
set_from_resource $gray		color8	#808080

# Indicator
set_from_resource $magenta	color13	#ff00ff
set_from_resource $dmagenta	color5	#800080

# Selection
set_from_resource $green	color10	#00ff00
set_from_resource $dgreen	color2	#008000

# Urgent
set_from_resource $red		color9	#ff0000
set_from_resource $dred		color1	#800000


# BINDINGS #
# start a terminal
bindsym $mod+Return exec $TERMINAL

# kill focused window
bindsym $mod+Shift+q kill

# program launcher
#bindsym $mod+d exec "rofi -lines 3 -columns 3 -modi run,ssh -config \\"~/.config/rofi/roficonf\\" -show"
bindsym $mod+d exec dmenu_run -fn "xos4 Terminus:size=16:style=Bold" -nb "$white" -nf "$black" -sb "$green" -sf "$black" -i

# Emoji selector
bindsym $mod+e exec ~/.scripts/emoji

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left 30
bindsym $mod+Shift+Down move down 30
bindsym $mod+Shift+Up move up 30
bindsym $mod+Shift+Right move right 30

# Vim keys for some reason
bindsym $mod+Shift+h move left 30
bindsym $mod+Shift+j move down 30
bindsym $mod+Shift+k move up 30
bindsym $mod+Shift+l move right 30
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# split orientation
bindsym $mod+v split h
bindsym $mod+c split v

bindsym $mod+f fullscreen toggle

# Toggle floating and change borders depending on the mode
bindsym $mod+Shift+space floating toggle;

bindsym $mod+space focus mode_toggle

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10:0"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# Alternatively switch to next or previous workspace with tab
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# Extra keys
bindsym XF86AudioPause exec playerctl play-pause && exec pkill -SIGRTMIN+13 i3blocks
bindsym XF86AudioPrev exec playerctl previous && exec pkill -SIGRTMIN+13 i3blocks
bindsym XF86AudioNext exec playerctl next && exec pkill -SIGRTMIN+13 i3blocks
bindsym XF86AudioLowerVolume exec amixer sset Master 5%- && exec pkill -SIGRTMIN+12 i3blocks
bindsym XF86AudioRaiseVolume exec amixer sset Master 5%+ && exec pkill -SIGRTMIN+12 i3blocks
bindsym XF86AudioMute exec amixer sset Master toggle && exec pkill -SIGRTMIN+12 i3blocks
bindsym XF86HomePage exec firefox
bindsym Print exec scrot


# Mouse support
# The middle button and a modifer over any part of the window kills the window
# Until issue github.com/i3/i3/issues/3459 is fixed, this will have to do
bindsym --whole-window $mod+button2 kill
# bindsym --release --whole-window $mod+button2 kill

# The right button toggles floating
bindsym button3 floating toggle
bindsym $mod+button3 floating toggle


# resize window (you can also use the mouse for that)
mode "resize" {
	bindsym Left resize shrink width 100 px or 10 ppt
	bindsym Down resize grow height 100 px or 10 ppt
	bindsym Up resize shrink height 100 px or 10 ppt
	bindsym Right resize grow width 100 px or 10 ppt

	bindsym $mod+Left resize shrink width 10 px or 1 ppt
	bindsym $mod+Down resize grow height 10 px or 1 ppt
	bindsym $mod+Up resize shrink height 10 px or 1 ppt
	bindsym $mod+Right resize grow width 10 px or 1 ppt

	# Vim keys for some reason
	bindsym h resize shrink width 100 px or 10 ppt
	bindsym j resize grow height 100 px or 10 ppt
	bindsym k resize shrink height 100 px or 10 ppt
	bindsym l resize grow width 100 px or 10 ppt
	bindsym $mod+h resize shrink width 10 px or 1 ppt
	bindsym $mod+j resize grow height 10 px or 1 ppt
	bindsym $mod+k resize shrink height 10 px or 1 ppt
	bindsym $mod+l resize grow width 10 px or 1 ppt

	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# System controls in bar
set $mode_system System (l)ock, (e)xit, (s)uspend, (r)eboot, (p)ower-off
mode "$mode_system" {
	bindsym l exec i3lock -ec 141414, mode "default"
	bindsym e exec i3-msg exit, mode "default"
	bindsym s exec i3lock -ec 141414 && systemctl suspend, mode "default"
	bindsym r exec systemctl reboot, mode "default"
	bindsym p exec systemctl poweroff, mode "default"

	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym m	   mode "default"
	bindsym $mod+m mode "default"
}
bindsym $mod+m mode "$mode_system"


# <colorclass>		<border><background><text>	<indicator>	<child_border>
client.focused		$white		$white	$black	$magenta	$white
client.focused_inactive $gray	$gray	$black	$dmagenta	$gray
client.unfocused	$gray		$gray	$black	$dmagenta	$gray
client.urgent		$black		$black	$black	$white		$red

bar {
	status_command i3blocks -c ~/.config/i3/i3blocks.conf
	position top
	separator_symbol " "
	tray_padding 0
	strip_workspace_numbers yes

	#tray_output none	# Find a udiskie replacement first

	colors {
		background	$black
		statusline	$white

		#<colorclass>		<border><background><text>
		focused_workspace	$black	$black	$white
		inactive_workspace	$black	$black	$gray
		urgent_workspace	$dred	$dred	$white
	}
}

# Execute on startup
exec udiskie -ans2
exec firefox

# Execute on each load
exec_always xrdb ~/.Xresources
